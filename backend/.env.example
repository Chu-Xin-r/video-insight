# ========== 大模型 API 配置（OpenAI 兼容）==========
# Key 获取优先级：环境变量 -> ~/.dsh/.credentials.yaml（DSH 凭据）-> 网页设置页自定义
# 在环境变量或 DSH 凭据中设置你的 Key（键名与 data/providers.json 中的 api_key_env 对应）：
# 例如：
# MY_LLM_API_KEY=sk-xxx
